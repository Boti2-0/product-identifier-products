spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/apostas
    username: cotas
    password: cotasApostas
    driver-class-name: org.postgresql.Driver
    hikari:
      poolName: Hikari
      auto-commit: false
      connection-timeout: 30000
      idle-timeout: 60000
      max-lifetime: 1800000
      maximum-pool-size: 100
      minimum-idle: 10